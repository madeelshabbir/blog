apiVersion: v1
kind: Service
metadata:
  name: ext-comment-srv
spec:
  type: NodePort
  selector:
    app: comment
  ports:
    - name: comment
      protocol: TCP
      port: 3002
      targetPort: 3002
